App
    Header (Header)
        Nav (Nav)
    Main (Main)
        Home (Section) // Input + boton para user 
        Form (Section) // Dar alta noticias -> redirect to /ListNews
        ListNews (Section) // Precarga de noticias API + Noticias alta de form
            Card   (Article) // Carga de cada noticia
    Footer (Footer)


